{
  "type": "module",
  "scripts": {
    "setup": "git submodule init && git submodule foreach git submodule init && git submodule foreach git submodule update"
  },
  "dependencies": {
    "@webgpu/types": "^0.1.13"
  }
}
